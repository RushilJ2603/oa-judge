1. so the question was , we were given a stirng s, a duplicatepenalty, and a segmentpenalty
every element that is same as its adjacent would have a penalty of duplicate penalty, and we can split it with segment penalty, we have to find the minimum penalty for the entire string

i tried dp with i and j , partition dp,

2. there was a set of 2n numbers from 1 to 2n, we had to make n bundles of two elements each, and there is a target array of elements to be formed

within a bundle we can pick either its min element or its max element, for example 1 3 5 is the target array, we can make bundles form target.size()*2, so 1 to 6, now we can bundle it like this, (1,4), (3,2), (5,6)
such that lets pick min from bundles 1 and 3, and max from bundle 2, so that will give the target array, now that meant we picked min in 2 bundles, pick the number of mins we can pick that would still get us the target sum, n-x bundles would pick max then, so its like can you make the target array using 2 min bundle picking, 1 min bundle, rest max, 4 min , rest max, etc etc

3. there is a string of 0s and 1s, we can flip any consecutive subset of that string, we have to find out the number of different strings that we can form